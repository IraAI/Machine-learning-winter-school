function [ss,sigma]=scalespace(img,levels,step)

sigma = ones(1, levels);
for i= 2:levels
    sigma(i) = step*sigma(i-1);
end

ss(:,:,1) = img;
S = (x, y, sigmai+1)
ss(:,:,2) = S;

S =

ss = (:,:, S));



end